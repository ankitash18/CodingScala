package DFbasics

/**
  * Created by AShrivastava on 12/8/2019.
  */
object Sets extends App{

  /*val year = 2015

  val natinaldata = {
    val source = io.Source.fromFile("src/main/resources/data/yob2015.txt")
     val names=source.getLines().filter(_.nonEmpty).map(_.split(",")(0)).toArray

      source.close()
       names
  }
  val info =for(statefile <- new java.io.File("src/main/resources/data").list();
  if statefile.endsWith(".TXT")) yield {

    val source =io.Source.fromFile("src/main/resources/data/"+statefile)

     val names = source.getLines().filter(_.nonEmpty).map(_.split(",")).filter(a => a(2).toInt ==year).map(a => a(3)).toArray

     source.close()
    (statefile.take(2),names)


  }

  for((state,snames) <- info){


  }
  */
}
